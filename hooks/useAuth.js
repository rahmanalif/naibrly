/**
 * useAuth Hook
 * Custom hook for authentication state management
 * Re-exported from AuthContext for better organization
 */

import { useAuth } from '@/context/AuthContext';

export default useAuth;
